name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
    - name: install the Docker 
      run: |
          set +e
          brew install colima
          brew install docker
          colima start
          docker build .


